
/**
 * Header component:
 * Defines the props needed for the header
 */

const Header = ({title}) => {

  return(
    <header className="header dafe-in">
      <h1>{title}</h1>
    </header>
  )
}

export default Header
